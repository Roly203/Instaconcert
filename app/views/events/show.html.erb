<% require 'json' %>

<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @event.name %>
  <%= @instagram_loc_id %>
  <%= JSON.pretty_generate (@instagram_data_array) %>
  <ul>
  <% @instagram_data_array.each_with_index do |item,i| %>
    <li><img src="<%= @instagram_data_array[i]["images"]["standard_resolution"]["url"] %>"></img></li>
  <% end %>
  </ul>

</p>


<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
